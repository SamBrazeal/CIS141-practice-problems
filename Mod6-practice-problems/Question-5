#5. Create a list of integers. Use a loop to build a new list where each element is the square
#of the corresponding element in the original list. Print the new list.
numbers = [1, 3, 5, 7, 9]

squared_numbers = []

for num in numbers:
    squared_numbers.append(num ** 2)

print("Squared numbers:", squared_numbers)
